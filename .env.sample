# Sample environment variables for local testing
# Copy this file to .env and update with your actual values

# Application Mode: orchestrator or runner
MODE=runner

# SQL Server Connection String
# For Azure SQL Database:
CONN_STR=Server=your-server.database.windows.net;Database=your-database;User Id=your-user;Password=your-password;Encrypt=true;TrustServerCertificate=false;

# For SQL Server with Windows Authentication (local testing):
# CONN_STR=Server=localhost;Database=master;Integrated Security=true;

# Required for orchestrator mode only
IMAGE_NAME=your-registry.azurecr.io/sql-job-runner:latest
NAMESPACE=default
SECRET_NAME=sql-connection-secret
